# ============================================================
# 主升浪加速行情自动扫描与决策 Bot 系统 - 依赖配置 v4.0
# ============================================================
# v4.0: 移除所有国内专用数据源, 全部改用腾讯/新浪 HTTP API
# 确保 GitHub Actions 海外环境 100% 可用
# ============================================================

# 数据获取层 (纯 HTTP, 不封 IP)
requests>=2.31.0          # HTTP 请求 (腾讯财经 + 新浪财经 API)
pandas>=2.2.0             # 数据清洗/时序处理/指标计算
numpy>=1.26.0             # 数值计算

# 技术指标计算
ta>=0.11.0                # MACD/RSI/Bollinger/MA 等 40+ 指标 (纯 Python)

# 通知推送 (通过 requests 直接调用 Webhook)
python-dotenv>=1.0.0      # 环境变量管理
loguru>=0.7.2             # 日志框架 (支持文件轮转)
tabulate>=0.9.0           # 终端表格输出
pytz>=2024.1              # 时区处理
pyyaml>=6.0               # YAML 解析
openpyxl>=3.1.0           # Excel 导出 (报告)
